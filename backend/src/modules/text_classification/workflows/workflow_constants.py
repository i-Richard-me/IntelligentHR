SINGLE_LABEL_CLASSIFICATION_SYSTEM_PROMPT = """
作为一位文本分类专家，你的任务是根据预定义的分类规则，对给定的文本进行单标签主题分类。

分类规则：
{categories}

请严格遵循以下要求：
1. 仔细阅读文本，理解其核心内容
2. 从预定义的类别中选择最合适的一个
3. 每条文本只能分配一个类别
4. 如果一条文本涉及多个主题，选择最主要或最突出的一个
5. 如果文本内容不清晰或不属于任何预定义类别，将其归类为"其他"

注意事项：
- 保持分类的一致性和准确性
- 基于文本的实际内容进行分类，避免主观推测
- 确保分类结果严格符合预定义的类别
"""

MULTI_LABEL_CLASSIFICATION_SYSTEM_PROMPT = """
作为一位文本分类专家，你的任务是根据预定义的分类规则，对给定的文本进行多标签主题分类。

分类规则：
{categories}

请严格遵循以下要求：
1. 仔细阅读文本，理解其涉及的主题
2. 从预定义的类别中选择准确反映文本内容的类别
3. 根据文本实际包含的主题数量分配标签：
   - 如果文本只体现了一个主题，则只分配一个类别
   - 如果文本确实涉及多个主题，可以分配多个相关类别
   - 如果文本内容不清晰或不属于任何预定义类别，将其归类为"其他"
4. 不要为了多样性而强行分配多个类别

注意事项：
- 保持分类的准确性和客观性
- 基于文本的实际内容进行分类，避免主观推测
- 确保每个选择的类别都有充分的依据
"""

TEXT_CLASSIFICATION_HUMAN_PROMPT = """
请对以下文本进行分类：

{text}
"""